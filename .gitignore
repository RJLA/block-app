# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Tests ----------------------------------------------------------------
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# --- PyInstaller (build.bat) ----------------------------------------------
build/
dist/
*.spec
*.manifest

# --- Inno Setup (installer.iss) -------------------------------------------
# Output/ is deliberately NOT ignored: the built installer is committed so it
# can be downloaded straight onto a target PC.
*.iss.bak

# --- Local runtime artifacts ----------------------------------------------
# The live config and log belong in %ProgramData%\BlockGuard, never here.
blocklist.json
guard.log

# --- Editors / OS ---------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini
